# Dreaming 复盘 2026-09-20（周日）

## 0. 今日概况（事实层）

- **今日 CFO 零业务交互**：workspace 下无当日 memory 文件；`~/.openclaw/agents/cfo/sessions/` 当日唯一写入是本次 22:01 的 dreaming 运行本身。无飞书收发、无礼单录入、无合同或审账动作。
- **中秋礼单表（逸凡自行操作）**：总记录 **327**（9/19 324 → +3）。顶层 313 + 子 14。地址复核确认 **272**（顶层 258 + 子 14）| 已邮寄/交付 **250** | 已打包 **257** | 有邮寄编号 **251**。序号 max=**367**，缺口 **40 个**（较 9/19 新增缺口 366）。变更对账 vs snap_2026-09-19：新增 3 / 消失 0 / 字段变化 0。
- **新增 3 条来源为逸凡表内自建**（无会话记录、非我所建）：364 刘新宇（春光-承礼）| 365 和月（卡乐比直客）| 367 安久（海马体直客）。三条收件信息均为多行。
- **序号 366 为创建后删除**：364 至 367 全属今日新段，366 不在册，AutoNumber 只在删除后留缺口。操作人与时间不可考（不经我手）。
- **cron 现状（实读）**：lastRunAtMs = 2026-09-19 22:15:50（上一轮撞限后的自动重试），status = ok，duration = **56.5s**；三项配置零变化：payload_timeout_seconds = 300、delivery_mode = none、schedule_expr = 1 22 * * *。今日 22:01 首跑正在执行。**挂账第 31 天。**
- 脚本守护仍生效：`rec_api.py progress` 实跑 327，与 `audit.py` 同源一致（守卫U 的写后读数与单一实现未被破坏）。

## A. 执行错误

**A1（P0，第 31 天未修，唯一有实际损害的悬项）：cron 超时上限与告警口未修，昨日已发生真实截断。**

9/19 首轮 durationMs = 300143、status = error、errorReason = timeout，是挂账以来上限第一次真正截断；同日产生第二次全量重跑（同日双跑，纯重复 token）。今日首跑虽正常启动，但根因未除：300s 余量在 9/17（219.6s）、9/18（210.3s）已逼近。今日风险低只因零业务、上下文小，一旦有业务日必再撞。这不是今天的错误，是持续未闭环的系统缺陷，且已经用掉一次实际损害配额。

**A2（P2，第 4 天）：格式校验存在单向盲区，今日被放大。**

`rec_api.py warn_format()` 只在我写入时校验收件信息首段与换行；逸凡表内自建不经此路径。证据：今日新增 3 条全部多行，换行存量由 13 升到 **16 条**；同时存量矛盾 4 条（5/264/300/308 已勾复核但无收件信息）仍未动。

**A3（P2，新识别）：创建后删除的记录对各账不可见。**

序号 366 今日成为新缺口，而变更对账此前只报新增/消失/字段变化，不报缺口增量，读者要自行比对 40 个缺口列表才能发现有一条记录被建后即删。**本轮已在会话内修复，见 D2。**

**A4（认知纠偏，非执行错误）：9/19 对 cron 存储位置的结论不完整。**

9/19 记 cron 作业既不在 openclaw.json 也不在 ~/.openclaw/cron。前半正确，后半遗漏了真正的存储地：**`~/.openclaw/state/openclaw.sqlite` 的 `cron_jobs` 表**（store_key 字段仍指向已废弃的 `~/.openclaw/cron/jobs.json` 路径，该文件只剩 .migrated/.bak）。影响：只写否定、不写坐标，下一个 session 仍会重复搜寻。

## B. 规避方案

**B1 cron 两项安全修复走网关，不再靠文字推送。**

存储位置已精确定位：`cron_jobs` 表，列 payload_timeout_seconds（改 600）与 failure_alert_after（置 3）及告警投递列。**不采用直接改 sqlite 的方式**：该库由网关进程持有，外部写入可能被内存态覆盖或造成不一致，且调度器变更未获授权。正确入口是主 session 的 cron update（网关中介）。绑定决策点：此项不写进常规复盘正文，而是并入 9/22（周一）的全量未闭环推送，以闭合选项给出（回改即由主 session 执行 timeout 与 failureAlert；schedule 频率另需一句确认）。

**B2 格式盲区改为表主视角的只读巡检。**

audit.py 已有收件信息含换行与已确认但无收件信息两项扫描，覆盖逸凡自建记录。处置：不为存量 16 条换行加 4 条矛盾逐条推送（噪声比过高，守卫Q）；改为 9/22 推送时给出两条默认定性（建议一键归一化为单行 / 补录或撤勾）。

**B3 缺口增量并入变更对账。**（本轮已落地）

audit.py 的 diff 段新增新增缺口与补齐缺口两行，直接读出 366 这类建后即删的痕迹，避免读者自行比对 40 个缺口。与 9/19 守卫U 同路径：改脚本并当场跑反例验证，不写成文字规则。

**B4 事实纠偏即刻写回。**

9/19 的 cron 位置描述已在 MEMORY.md 更正（见本轮 MEMORY 更新）。

## C. 书写质量

- 今日无一封对外回复，无风格违规样本可评。
- 本复盘文件按 SOUL 全局风格自查：未使用装饰性引号、破折号与 AI 填充词；结论加粗；结构化分层。
- 上一轮（9/19）遗留的可优化项延续：回执只报确认与已邮寄两个数，不夹带其他计数。今日无回执，留待下次业务日执行。

## D. 改进计划（明天 9/21 周一）

1. **cron 修复推到手边**：并入 9/22 周一全量推送，闭合选项（timeout 600 与 failureAlert after3，回改即执行；schedule 频率需一句确认）。在此之前不再单独重复第 7 次。
2. **audit.py 加缺口增量**：**已在本次会话内完成并跑反例验证**（输出 新增缺口 1 个: [366]）。
3. **存量数据质量**：不逐条推（噪声比），9/22 一并给默认定性。
4. **表主自建记录的格式问题**：维持扫描可见即可，不追着改。

## 附：本轮验证输出（守卫N/T 自证）

- `python3 -m py_compile audit.py` → PY_COMPILE_OK
- 从 /tmp 运行 `audit.py`（验证绝对路径，守卫L）：变更对账 vs snap_2026-09-19.json 段新增一行 `(缺口) 新增缺口 1 个: [366]`
- `rec_api.py progress` 实跑：总记录 327 | 顶层 313 | 子 14 | 确认 272（顶层258+子14）| 已邮寄/交付 250 | 已打包 257 | 有邮寄编号 251

## Reflection

- **Goal**：复盘本身达成；今日零业务，无用户问题待解决。真正的目标（消除 cron 截断风险）未达成，已 31 天。
- **Key Decision**：拒绝直接改 sqlite 修 cron，改走网关中介加闭合选项推送。依据是调度器变更未授权、外部写网关库有一致性风险。
- **Main Mistake**：今日无新增执行错误；可算的是 9/19 对 cron 存储位置的描述不完整，本轮已纠偏。若论模式，连续 31 天把同一个已知修复留在待办才是最大的问题。
- **Repeat Risk**：是。cron 悬项属高频、低成本修复、判断层已明确的事项，是系统性未闭环，非一次性失误。
- **Memory Update**：见本轮 MEMORY.md 新增 9/20 区块（零交互日事实、表内自建 3 条加 366 建后即删、cron 存储位置纠偏、换行存量升到 16、缺口增量已编译）。
- **Principle Update**：新增 **守卫V 可达性结论必须给出确切路径坐标**。凡写不在 A 也不在 B 的负面结论，必须同时给出实际在 C 或明确声明未找到；只写否定等于让下一个 session 重复搜寻。依据 9/19 的 cron 位置描述。
