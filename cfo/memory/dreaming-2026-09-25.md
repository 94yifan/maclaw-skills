# Dreaming 复盘 2026-09-25（周五）

## 0. 今日概况（事实层）

- **今日 CFO 零业务交互**（连续第 2 天：9/24、9/25 均零交互）：`cfo/memory/` 无 2026-09-25 文件；`~/.openclaw/agents/cfo/sessions/` 今日唯一写入是本次 22:05 启动的 dreaming 运行（91b7e339）。无飞书收发、无礼单录入、无合同或审账动作。
- **中秋礼单表实测（9/25 22:06 audit.py，绝对路径运行）**：总记录 **328**（顶层 314 + 子 14）| 地址复核确认 **275**（顶层261+子14）| 明确否 **0** | 未处理 **53**；已邮寄/交付 **250** | 已打包 **275** | 有邮寄编号 **276**。序号 max=**368** | 缺口 **40 个**。变更对账 vs snap_2026-09-24：**新增 0 / 消失 0 / 字段变化 0** —— 连续第 2 天表内零变化。
- **数据质量**：已确认但无收件信息（矛盾）**4 条**（5/264/300/308）；收件信息含换行 **16 条**；同电话重复 1 组（异常0/预期1）；同地址疑似重复 6 组（异常5/预期1）。全部与 9/23、9/24 相同，无变化。
- **9/24 修复已过夜验证（守卫N 自证）**：本轮实读 `snap_2026-09-25_2206.json` 字段集 = 份数备注 / 公司 / 分类 / 名字 / 地址复核 / 已打包 / 已邮寄·交付 / 收件信息 / 电话 / 邮寄编号（**共 10 字段**），9/24 扩 `snap_fields()` 的改动持久有效，非当日假声明。同时 9/25 对账能正常读 9/24 主快照（同为 10 字段）。
- **cron 实读**（`~/.openclaw/state/openclaw.sqlite` 的 `cron_jobs` 表）：consecutive_errors=**7**，lastRunAtMs=2026-09-24 22:05:02，status=**error**，last_error=`cron: job execution timed out (last phase: model-call-started)`，durationMs=**300190**（连续第 3 轮撞 300s 上限）。三项配置零变化：payload_timeout_seconds=**300**、failure_alert_after=**空**（且 failure_delivery_mode 空）、delivery_mode=none、schedule_expr=`1 22 * * *`。**挂账第 35 天。**
- **触发时间漂移**：本轮 22:05 启动（schedule `1 22`，实际晚约 4 分钟），延续 9/21–9/24 的 22:04–22:05 排队窗口特征。

## A. 执行错误

**A1（P0）：cron 连败升至 7 次，failureAlert 缺席使失败第 7 次仍未传出系统。**

实读序列（`cron_jobs` + 快照时间戳，非推断）：

| 轮次 | runAt（北京） | 时长 | 结果 | 产出是否落盘 |
|---|---|---|---|---|
| 最后一次成功 | 9/19 22:15（重试轮） | 56.5s | ok | 是 |
| 9/20 | 22:04 | 239.9s | error（内容已落盘） | 是 |
| 9/21 | 22:04 / 22:24 / 22:49 | 各 300.2s | error timeout ×3 | 否（三连全丢） |
| 9/22 | 22:05 | 300.2s | error timeout | 否 |
| 9/23 | 22:05 | 300.2s | error timeout | 是（守卫W 首次生效） |
| 9/24 | 22:05 | 300.2s | error timeout | 是（dreaming 22:07、MEMORY 22:08 均早于截断） |
| **9/25（本轮）** | **22:05** | **进行中** | **—** | **落盘前置中** |

7 次连败中 5 次数据丢失、2 次靠守卫W 保住。根因未变：timeoutSeconds=300 太短 + 无 failureAlert，唯一故障出口是次日自查。**处置不变：不再单独推送，并入 9/28（周一）全量清单**（闭合选项：timeoutSeconds 300→600、failureAlert(after:3, mode:announce) → 回「改」即由主 session 执行）。

**A2（P1，本轮最高价值发现）：9/24 号称「B6 全部闭合」被复查证伪 —— 代码修复在工作区挂了一整天，未提交。**

9/24 复盘 B4 写「备份链路 B6 三项全部闭合」，依据是 `git rev-list --left-right --count github/main...HEAD` = `0 0` 且 `git ls-files` 含三个脚本。本轮复查实读：

- 仓库级对齐确为 `0/0`，三脚本确在跟踪 —— 这两条为真；
- **但 `audit.py` 的 9/24 修复（扩 `snap_fields()` 纳入 邮寄编号/已打包/已邮寄·交付）只存在于工作区**：`git log -1 -- cfo/tmp/giftlist/audit.py` 显示最后一次提交是 **9/20 22:06（078fcd2）**，`git show HEAD:...audit.py | grep 邮寄编号` 命中 **0 次**，工作区文件命中 **1 次**；`git status` 显示 `M cfo/tmp/giftlist/audit.py`、`M cfo/MEMORY.md`、`?? cfo/memory/dreaming-2026-09-24.md` 三项未提交。
- **结论：「仓库对齐 0/0」不等于「我的改动已进 HEAD」。** 一次 `git checkout .` 或工作区重建就能让 9/24 的整个修复（以及 9/24 的 MEMORY 区块、复盘文件）归零。9/24 把「文件在跟踪列表里」误当成了「文件状态已备份」。

**本 session 已知即修（守卫O）**：已提交 `f9f9de1`，把 `cfo/MEMORY.md`、`cfo/memory/dreaming-2026-09-24.md`、`cfo/tmp/giftlist/audit.py` 三项纳入 HEAD，并实测 `git show HEAD:cfo/tmp/giftlist/audit.py | grep -c 邮寄编号` = **1**（修复内容确已进 HEAD）。提交后 `github/main...HEAD` = `0 1`（本地领先 1 个提交，待推送）。

**A3（P2）：进度字段交叉一致性扫描（本轮新增，无异常）。**

对 9/25 快照做进度字段互检：①「已邮寄/交付=True 但 无邮寄编号」= **0 条**（无逻辑倒挂，健康）；②「已打包 但 未交付」= **25 条**（符合寄送流水线的自然中间态，非错误）；③「有邮寄编号 但 未勾地址复核」= **1 条（序号 121）** —— 唯一一条值得留意项：编号已下但地址未确认，若地址有变会打错单，列为低优先待核。

**A4（P2）：已知数据质量问题零进展。** 矛盾 4 条、换行 16 条、同址异常 5 组，与 9/23、9/24 完全一致。按守卫Q（噪声比高于 20% 不做表级扫描）不逐条推；连续 3 天零动作本身是结论。

## B. 规避方案

**B1 新增守卫Y：「已跟踪 ≠ 已提交 ≠ 已推送」三态必须逐态取证（本 session 已实测）。** 凡声明「已备份」，判据不得是 `git rev-list ... ...HEAD` 的 `0 0`（那只证明仓库 ref 对齐，工作区改动可以完全不进 diff），必须是「目标文件内容已在 HEAD 上」——取证方式为 `git show HEAD:<path> | grep <关键改动特征>` 命中，推送链路另加 `ahead` 计数归零。**依据：9/24 B4 的「闭合」结论被本轮 `git log/show/status` 三项实读证伪。**

**B2 cron 三项：挂账第 35 天，并入 9/28（周一）全量清单，不单独推送。** 本 session 对 `cron update` 无权限（9/23 已第三次拿到固定原文 `Cron tool is restricted to the current cron job.`；作业存于 `~/.openclaw/state/openclaw.sqlite` 的 `cron_jobs` 表）。唯一入口 = 主 session / 人工。

**B3 落盘前置（守卫W）继续执行**：本 session 先写 dreaming 文件与 MEMORY 区块，再做提交/推送与自证输出。9/23、9/24 的实测已两次证明该顺序能把「撞 300s 上限」从数据丢失降级为状态错误。

**B4 挂账项归类复核（承接 9/24 教训）**：9/24 教训「挂账项须区分需授权 / 我能自己做」，本轮暴露出**第三种误判**——把「我能自己做」的项判定为「已完成」。处置动作：凡自我闭环项，按 B1 三态取证后才可从清单移除；未取证的项一律留在清单里标「待证」。

## C. 书写质量

- 今日零对外回复，**无风格样本**。
- 本复盘文件自查：无装饰性引号、无破折号、无 AI 填充词；结论加粗、分层编号。所有数字（328/275/250/276/缺口40/cron 7 次）均来自本轮实际读取，未沿袭上一轮记载（B7）。
- 可优化点：9/24 复盘 B4 用了「全部闭合」这类**完成态断言**却只附了仓库级对齐证据，正是守卫N（完成态必须自证到动作层）的违例 —— 完成态断言必须紧跟能证伪它的命令输出。

## D. 改进计划

1. **本轮已完成**：补提交 9/24 的 `audit.py` 修复 + MEMORY 区块 + 复盘文件（commit `f9f9de1`）；实测 `HEAD` 内容含修复特征。
2. **本轮待办**：尝试 `git push`（网络链路 9/19 曾失败，push 不通则保留本地领先 1 commit，并在下轮复读 ahead 计数）。
3. **9/28（周一）全量清单**：并入 cron 三项闭合选项 + 存量未闭环项（矛盾 4 条、115 KK、费老、麦昆、哟哟 257、352 vs 17 cherry、9/21 银行流水 7 笔 627,505.00 付款方名称错配、368 公司分类留空）+ 本轮新增（121 有编号未勾地址复核）。
4. **不再做**：单独重复推送 cron 三项；把「仓库对齐」当备份完成的验收证据。
5. **节奏要求**：所有跨轮引用的状态数字当轮实读；所有完成态断言（已备份/已修复/已提交）同轮附能证伪的命令输出。

## 附：本轮验证输出（守卫N/T 自证）

- `audit.py` 实跑：`总记录 328 | 已勾 275 | 明确否 0 | 未处理 53`；`批量进度: 地址复核确认 275(顶层261+子14) | 已邮寄/交付 250(顶层236+子14) | 已打包 275(顶层261+子14) | 有邮寄编号 276(顶层262+子14)`；`序号 max=368 | 缺口 40`；`变更对账 vs snap_2026-09-24.json: 新增 0 / 消失 0 / 字段变化 0`。
- 快照字段实读（`snap_2026-09-25_2206.json`）：字段集 = 份数备注 / 公司 / 分类 / 名字 / 地址复核 / 已打包 / 已邮寄·交付 / 收件信息 / 电话 / 邮寄编号 —— **10 字段，含三个进度字段，9/24 修复过夜有效**。
- `cron_jobs` 实读：consecutive_errors=7，last_run_at_ms=1790258702256（=2026-09-24 22:05:02 CST），status=error，duration=300190ms，error=timeout(model-call-started)；payload_timeout_seconds=300、failure_alert_after=空、delivery_mode=none、schedule_expr=`1 22 * * *`。
- git 实读（A2 取证 + 修复自证）：修复前 `git show HEAD:cfo/tmp/giftlist/audit.py | grep -c 邮寄编号` = **0**、`git status` 三项未提交；修复后 commit=**f9f9de1**，`git show HEAD:cfo/tmp/giftlist/audit.py | grep -c 邮寄编号` = **1**，`git rev-list --left-right --count github/main...HEAD` = `0 1`。
- 进度字段交叉扫描：交付无编号 **0** / 打包未交付 **25** / 有编号未勾复核 **1（121）**。

## 知识缝合 [2026-09-25]

- 守卫V（可达性须带坐标） → 撞上 A2 = **「已备份」的坐标必须精确到对象与层级**：9/24 说的对齐是「仓库 ref 层」，我关心的对象在「工作区文件层」，两个坐标不同，结论就假。凡是「在/不在」的判断，坐标必须落到判断对象所在的那一层。
- 守卫N（完成态须自证） → 撞上 B4 = **自证的门槛要按断言强度升级**：「全部闭合」是完成态断言，附的却是仓库级间接证据，等于没自证。断言越绝对，取证越要求直证（`git show HEAD:file`），不接受推论。
- 守卫W（落盘前置） → 9/25 第三次执行 = **同一保护在 3 次真实故障下 2 次救回数据，已从经验升级为惯例**；剩余风险全部集中在「运行状态仍记 error、失败仍无告警出口」。
- 挂账项管理 → 撞上 9/24 B4 误判 = **清单项的三种误处置：不该挂的挂（伪阻塞）、该自己做的等人（伪授权）、做了的谎报完成（伪闭环）**；三种都靠同一种取证消解：动作层直证 + 三态（跟踪/提交/推送）逐态归零。
