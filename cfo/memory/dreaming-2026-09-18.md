# Dreaming 复盘 2026-09-18（周五）

## 今日性质判定：高交互日（13 轮）

逸凡全天 12:10 → 20:08 全部围绕中秋礼单：6 条新建、3 条 update、2 次计数询问、1 次规则下达。工具通道健康：飞书 API 0 次 400（业务链路），所有序号从 create 返回体或 get 读回。

取证来源：`agents/cfo/sessions/ef4e624d-8c11-490b-a1f4-7dbe6263a49a.jsonl`（13 条 user 消息 = 13 轮，12:10:54–20:08:53 UTC+8）+ 同名 `.trajectory.jsonl` 全部 exec 命令与结果。今日无 `memory/2026-09-18.md`（当日没有写日常 memory 文件），上下文来自 MEMORY.md 的 09-18 区块 + 会话记录重建。

**当日交互清单**

| 北京时间 | 逸凡指令 | 我的处理 | 结果 |
|---|---|---|---|
| 12:10 | Cherry总 新金桥 + 地址 | findmany 5 关键字全空 → 新建 352；首条回复**未推未闭环清单** | 读回 352 |
| 13:42 | 张处 团中央 + 地址 + 备注 | 查无 → 新建 353（份数备注照原话写） | 读回 353 |
| 13:45 | 地址确认多少份了 | 跑 audit.py，回「236 条 / 折算 252 份」**双口径并列** | 口径未被接受 |
| 13:47 | 不要管数字记录 只按照父子记录算 | 手搓 cfo_pc~pc5 五个脚本 | 全表 317 / 236 |
| 13:49 | —（我主动补） | 回 236，并追加「父子折叠是 222」 | 多余 |
| 14:06 | 现在确认多少了 | 回 241（顶层 227 + 子 14） | 接受 |
| 14:07 | 焦姐 潮凤 + 地址 | 查无 → 新建 354 | 读回 354 |
| 14:11 | 每次新增之后告诉我确认多少 邮寄多少 | 写 cfo_stat.py，回 确认 242 / 已邮寄 215；MEMORY 落硬规则 | 规则生效 |
| 15:50 | 昂扬地址确认 | find → update 129 勾复核 | 读回 |
| 16:04 | 王总 瑞幸 + 地址 | 查无 → 新建 356 | 读回 356 |
| 18:28 | 玲玲 小红书渠道赋能 + 新址 | update 181（地址+公司），回执列原值/新值 | 读回 |
| 19:19 | 健良地址不变 | find → update 135 勾复核 | 读回 |
| 19:52 | Clary 唯宝卫浴 品牌 + 地址 | 查无 → 新建 360（分类品牌） | 读回 360 |
| 20:08 | 建立叔叔 天翼 + 地址 | 查无 → 新建 361 | 读回 361 |

---

## 表结构实测（22:05，从 /tmp 运行 audit.py 以验证绝对路径）

- 总记录 **322** | 已勾 **265** | 明确否 **0** | 未处理 **57**
- 顶层 308 + 子记录 14
- 地址复核确认 265（顶层251+子14）| 已邮寄/交付 250（顶层236+子14）| 已打包 250 | 有邮寄编号 251（顶层237+子14）
- 序号 max=**361** | 缺口 **39 个**（较 9/17 新增缺口 47/358/359）
- 同电话重复 1 组（异常 0 / 预期 1）；同地址疑似重复 6 组（异常 5 / 预期 1）
- 变更对账 vs snap_2026-09-17：**新增 9 条 / 消失 3 条 / 字段变化 28 条**

---

## A. 执行错误

**A1（P1）为回答一个数字，走了临时脚本路线：8 次 exec、7 个 /tmp 脚本、1 次 400。**
13:48–14:12，为了拿到「确认了多少份」的顶层/子拆分，我在 /tmp 连写 cfo_pc.py、cfo_pc2、cfo_pc3、cfo_pc4、cfo_pc5（四分钟内五个版本）、cfo_dbg.py、cfo_stat.py。其中 cfo_dbg 撞了 400（内联 header 与 R.call 不一致），又花一次调用去诊断。根因不是能力问题：audit.py 第一行已经给出「按记录 265 份」，只是没有顶层/子拆分；正确动作是扩 audit.py 一处输出，我选择了手搓。这与 9/17 已定结论（口径编译进默认路径）直接冲突，也违反教训5（调试文件必须归档）。

**A2（P1）口径已经确认过，我还在并列报。**
9/15 逸凡明确「每条记录算一份，子记录也有礼品」；9/18 13:46 我又回「按记录 236 / 按份数备注折算 252」，她不得不回「不要管数字记录 只按照父子记录算」；13:49 我还追加「如果你要的是父子折叠，那是 222」。把她两轮时间花在一个已经定了的问题上。守卫I（口径可能歧义时先声明口径）我执行成了「每次都声明两种口径」，方向反了。

**A3（P2）状态数字静默换数。**
13:46 报「319 条全表」，13:49 报「全表 317 条」，两分钟内两个数，全程没解释差异。真实原因是逸凡在 13:47–13:50 删了序号 341、346。数字变了不是错，不说明原因才是错。

**A4（P2）9/17 定下的两项当天没执行——其中一项是第三日复发。**
①「find/findmany 输出加分类列」未做（挂账 1 天，本轮已补）；②「回读固定 5 项（序号/名字/公司/分类/收件信息）」6 次新建只有 352、360 的两条回读带了分类，353/354/356/361 的「分类暂空」属于没读就写。9/17 的 A4 是同一条，9/16 的 A4 也是同一条。

**A5（P2）字段级对账只报「变了什么」，不报「变得对不对」。**
序号 5（Kelly Zhao）今天的字段变化是：收件信息 `王可 18292805261 …宏润国际花园东区10号楼-1802` → **空**，电话同步清空，而地址复核 `False → True`。这是一条自相矛盾记录（确认了地址却没有地址）。9/17 上线的字段级对账忠实记录了它，但没有任何判定，混在 28 条变化里过去了。直到今天给 audit.py 加了数据质量扫描才浮出来，同批还有 264 灵希、300 兰总、308（8号桥无名份）——**4 条「已确认但无收件信息」的顶层记录，全部寄不出去**。

**A6（P2）MEMORY.md 的 09-18 区块停在 14:12，后面六小时没入库。**
09-18 区块只记到 352/353/354 和基线读数 242/215。之后新建的 355–361、逸凡的 28 处字段改动、3 条记录消失，全都没进 MEMORY。SOUL 要求「每个重要进展或决策必须实时更新到 MEMORY.md」。今天的实际做法是「改了一次规则就不管了」，把记录责任全推给当晚 dreaming——而 dreaming 是唯一的补录窗口，一旦它失败（见 A9），当天就是空白。

**A7（P2）格式校验两个盲区，一个漏报一个看到没说。**
①`warn_format` 的首段比较是 `segs[0] == name.strip()`，大小写敏感：360 Clary 的收件信息首段是「clary」，与名字「Clary」实际同指，校验静默通过。②序号 135 健良的存量记录里，收件信息首段「健良」就等于名字列（守卫E 违规）。我 19:19 那条 update 前打印了原值、看见了，但回执里只写「地址复核已勾」，没提这一条。校验漏报是脚本问题，看到了不说是我自己的问题。

**A8（P3）/tmp 卫生。**
单日新增 7 个调试脚本，`ls /tmp/*.py` 从 5 个涨到 12 个。本轮已全部回收（trash），审计/计数能力统一收进 `tmp/giftlist/`。

**A9（P3）cron 上一轮（9/17）状态是 error，且没有任何出口。**
读 job 实测：`lastRunStatus=error`、`consecutiveErrors=1`、`lastDurationMs=219617ms`、`lastDiagnostics.summary = "⚠️ 📝 Edit: in .../cfo/MEMORY.md failed"`（ts=9/17 22:04:51）。即 9/17 轮结尾一次 MEMORY.md Edit 失败，整轮被判 error。因为 `delivery.mode=none` 且无 failureAlert，**这条失败从未传到逸凡面前**——是我今天主动读 job 才发现的。经核查 9/17 的 MEMORY 内容已完整落盘，失败的那次 Edit 是冗余重写，没有造成数据缺失，但性质严重：挂账 29 天的 failureAlert，今天第一次拿到了「缺了它会导致什么」的实证。

---

## B. 规避方案（根因层）

**B1 本轮实际编译进脚本的改动（已逐条实跑验证，见 B2）**

- `audit.py` 新增「顶层 / 子记录」拆分 + 「批量进度」行：地址复核确认 / 已邮寄交付 / 已打包 / 有邮寄编号，每项带 `顶层N+子M`。
  **验证**：从 `/tmp` 运行输出 `批量进度: 地址复核确认 265(顶层251+子14) | 已邮寄/交付 250(顶层236+子14) | 已打包 250(顶层236+子14) | 有邮寄编号 251(顶层237+子14)`，与 cfo_stat.py 的历史输出完全一致 → 可以替代它，消除第二份实现。
- `audit.py` 新增「数据质量扫描」两行：
  ① 已确认但无收件信息（顶层，矛盾）；② 收件信息含换行（破坏「一行可复制打单」）。
  **验证**：实跑报出 ①4 条 `['5','264','300','308']`、②12 条 `['70','231','279','283','291','312','322','328','335','338','339','357']`，与手工核算一致。
- `audit.py` 质量扫描里写死一条说明：**「收件信息首段==名字」不做表级校验**。实测该规则表级扫描命中 90+ 条（存量表大量记录的名字本身就是签收人），上线即等于没有校验。把「为什么不查」写进脚本，防止未来的我再加一条噪声扫描。
- `rec_api.py` `warn_format` 比较改为归一化（去全部空白 + 大小写不敏感，`re.sub(r"\s+","",x).casefold()`），并在注释里写明反例（360 Clary/clary）。
  **验证**：三个反例全部告警——`Clary/clary` 大小写不同、`星然\n…` 换行+首段等于名字、名字含全角括号；合规用例（张处/张女士）静默。
- `rec_api.py` `find` / `findmany` 输出加 `分类=` 列（补 9/17 的 B4 待办）。
  **验证**：`rec_api.py findmany Clary Kristen` 输出 `… | 分类=品牌 | clary 13817321804 …`。以后回读 5 项（序号/名字/公司/分类/收件信息）一条命令完成。
- **回收**：`/tmp` 下 cfo_pc.py、cfo_pc2/3/4/5.py、cfo_dbg.py、cfo_stat.py 共 7 个已 trash；MEMORY 的硬规则改为只指向 `audit.py`。

**B2 守卫T：改脚本的「已编译」同样要当场自证（守卫N 的自然延伸）。**
本轮所有改动都在同一轮里附了真实命令输出：`python3 -m py_compile rec_api.py audit.py` 通过、warn_format 三反例+一静默、audit.py 从 `/tmp` 运行且快照落进 `snapshots/`（守卫L 未回归）、findmany 新列实际打印。**判据沿用 9/17：「已编译」不是声明，是一条本会话的命令输出。**

**B3 守卫P：口径一旦被确认就成唯一口径，不再并列（针对 A2）。**
口径未确认时并列 + 请她定（守卫I 原意）；口径已确认后只报那一个数。再并列等于把已决事项重新打开，成本由她承担。

**B4 守卫S：两次读数之间表变了，必须报增量与原因（针对 A3、A7）。**
凡上一次汇报过全表数、本次数又不同，必须写成「比上次 319 少 2 条，原因是逸凡删了 341/346」。数字变了要解释，不允许静默换数。

**B5 守卫Q：表级校验先算噪声量，噪声 >5 条不许做表级扫描（针对 A5 的反面）。**
上线任何表级扫描前先跑一遍看命中数，命中里「真需动作」的比例低于 20% 就不是校验，是噪音，这种扫描上线即失效。这条阻止的是「加了校验反而降低审查可信度」。

**B6 守卫R：计数口径编译进默认路径，不写临时脚本（针对 A1）。**
与 9/12、9/17 同源，但今天暴露了新的具体形态：不是「不想编译」，而是「临时脚本更快」。**约束写成可执行判据：凡为一个数字写第二个脚本，停下来先问能不能加进 audit.py；能加就加。**本轮已用 audit.py 的批量进度行兑现。

**B7 实时写 MEMORY，不留到当晚（针对 A6）。**
当天每完成一类动作（新建/update/发现异常）就落一行到 MEMORY 的当日区块，不依赖 dreaming 补录。dreaming 是校订，不是主写入通道——9/17 已证明 dreaming 本身会失败（A9）。

**B8 存量数据质量纳入固定输出（针对 A5）。**
「已确认但无收件信息」这类矛盾必须出现在每次 audit 输出里，而不是等有人想起来查。已编译进 audit.py。

---

## C. 书写质量

**做得好的：**
- **风格违规 0 条**（13 条回复，无装饰性引号、无破折号、无 AI 填充词）——连续第 3 天零违规（9/15=8/19，9/16=0，9/17=0，9/18=0）。
- 「已录入 ✅ + 序号/名字/公司/收件信息/复核/分类」的模板被 6 条新建复用，零格式漂移；收到 14:11 的新规则后，从 15:50 起每条回复都带「确认 / 已邮寄」两个实时读数，并注明口径，规则落地当天生效。
- 352 那条主动抛出同姓名待确认（352 Cherry总 vs 17 cherry，公司/签收人/地址全不同），没有默默按同人处理。
- 353 的份数备注照逸凡原话写（「增加大吃兄锅巴两袋（每种味道一袋）」），没有自作主张改写成数字。
- 21:28 玲玲那条把改动前后值、公司变更都列了，可追溯。
- 序号全部来自 create 返回体或 get 读回（守卫C 连续第 3 天完全生效）。

**需要改的：**
1. **✅ 用了 8/13 条回复**。SOUL 全局输出风格禁止项明确写着「禁止 emoji 装饰标记」。✅ 被我当成状态标记用了，但规则没有豁免条款，实际就是 emoji。此前几天未被计入违规，是自查只扫了「」/——/填充词三项。要么停用，要么在 SOUL 里写明豁免——**倾向停用**（规则写了就按规则执行，不为自己的习惯改规则），明天起「已录入」不带 ✅。
2. 「分类暂空」在 4 次新建里是没读就写（A4）。
3. 口径并列把已决事项重新打开（A2）。

---

## D. 改进计划（9/19）

1. **回复不带 ✅**：明天起所有回执用「已录入」+ 字段行，不加 emoji。
2. **新建后固定跑 `audit.py` 取两个读数**（确认 / 已邮寄），两个数字来自同一次读取，禁止沿用上一条消息里的数字。
3. **回读固定 5 项**：序号 / 名字 / 公司 / 分类 / 收件信息。find/findmany 现已带分类，一条命令即可完成。
4. **推送数据质量结论**：4 条矛盾记录（5/264/300/308）+ 序号 70 改三行格式 + 序号 5 地址被清空，给她一句话定性 + 默认动作（补录或撤勾）。
5. **cron 三项（挂账第 29 天，已有实证损害）**：`timeoutSeconds 300→600`、加 `failureAlert`（after:3, mode:announce）、schedule 频率待定。这轮不再是「待拍板」，是**明确授权请求**——9/17 轮已实际 error 且无人被告知，这是 failureAlert 缺席的直接后果。需主 session 执行。
6. **备份链路 B6②**：把 `cfo/tmp/giftlist/*.py` 纳入 git 跟踪（remote 已确认 94yifan/maclaw-skills，9/18 push 已追平）。脚本是资产，json 只是中间数据。
7. **全量未闭环清单下次推送 = 9/22 周一**（按 B5 每周一推）。9/19–9/21 只在出现阻碍当天动作的项时即时推。
8. **MEMORY 实时写**：当天动作完成后立即落一行，不等 dreaming（B7）。

---

## 状态核查（cron，仅记录不展开）

- 实测（本次会话读 job）：`timeoutSeconds=300`、`delivery.mode=none`（无 failureAlert）、`schedule=1 22 * * *`（Asia/Shanghai，每日）。三项零变化，**挂账第 29 天**。
- **9/17 轮实测状态：`lastRunStatus=error`、`consecutiveErrors=1`**，错误来自结尾一次 MEMORY.md Edit 失败（`lastDiagnostics.summary`，ts=1789653891962 = 9/17 22:04:51）。
- 时长归属校验：`lastRunAtMs=1789653672347` = **9/17 22:01:12**，`lastDurationMs=219617ms` = **9/17 轮值 219.6s**。
- 时长序列：9/7=45.5s、9/8=61.1s、9/9=53.6s、9/10=79.6s、9/11=140.2s、9/12=136.0s、9/13=117.4s、9/14=119.4s、9/15=127.4s、9/16=143.3s、**9/17=219.6s**。此前五轮稳在 117–143s，本轮跳到 219.6s（占 300s 上限 73%）——**上限余量在缩小，timeoutSeconds 300→600 的必要性从「理论风险」变成「趋势可见」**。
- `updatedAtMs=1789653891964` = 9/17 22:04:51，等于该轮结束时刻，判定为运行结束时自动回写，非配置变更。
- 本轮（9/18）`runningAtMs=1789740217005` = 22:03:37 启动；`nextRunAtMs=1789740060000` = 9/19 22:01。

---

## 知识缝合

- **今日新事实**（字段级对账 9/17 上线，今天遇到第一条真矛盾记录（序号 5：地址清空但勾复核），它只被记成 28 条中性变化中的一条）→ 关联 守卫K + 9/17「对账价值在于不静默」= **整合认知：不静默只解决了一半。对账要「不静默」，质量扫描要「有判定」。只会报「变了什么」的系统，会把「变坏了」和「变好了」同等展示，读者读到第 20 条就麻木。任何对账输出必须带一个二值判定。**
- **今日新事实**（差点把「首段==名字」写成表级校验，实测命中 90+ 条）→ 关联 守卫十（校验目标不得互斥）+ 9/15「开放问句本身是成本」= **整合认知：校验的性价比等于「真需动作的比例」，不是「覆盖的完整性」。命中 90 条里 0 条需要动作的扫描，副作用是让真正的 4 条被淹没——加一个坏校验，会让同一条输出里的好校验一起失效。**
- **今日新事实**（为 1 个数字写 7 个临时脚本、8 次 exec）→ 关联 9/12「默认路径优先」+ 守卫R = **整合认知：临时脚本不是「偷懒」，它比改脚本更快、更安全、更不用理解现有实现，所以是理性选择——这正是它难防的原因。防它的唯一办法是把成本反过来：让「加进 audit.py」比「新写一个」更快。**今天做成的方式是给 audit.py 加一行输出，成本 5 行代码。
- **今日新事实**（9/17 轮 error，原因是结尾 Edit MEMORY 失败，因无 failureAlert 无人知）→ 关联 挂账 29 天的 cron 三项 = **整合认知：一个悬而未决的配置项，代价不是「一直没修」，而是「你永远不知道它什么时候开始造成损失」。今天拿到的是这份账单的第一张收据。**
- **今日新事实**（口径 9/15 已确认，9/18 我又并列报）→ 关联 守卫I + 9/16 守卫M = **整合认知：规则本身正确，执行方向反了也会伤成本。守卫I 的适用条件是「口径未确认」，条件消失后它必须自动失效——否则正确的规则会变成把已决事项无限重开。**

## Reflection

- **Goal**：全天 13 轮业务处理完毕；6 条新建 + 3 条 update 全部读回；表内 322/265/0/57、新增 9/消失 3/字段变化 28 由审计自动产出；本轮新增 4 项脚本能力并逐条实跑验证。
- **Key Decision**：今天最重要的判断是把 audit.py 的四处改动（顶层拆分 + 批量进度 + 数据质量扫描 + 「为什么不查首段==名字」的说明）放在本次 dreaming 内做完并验证。其中「为什么不查」那条说明是反面资产——它阻止的下一个错误，比它修掉的那个更贵。
- **Main Mistake**：A1，为 1 个数字写 7 个临时脚本。它不是能力问题（audit 第一行就有答案），是路径依赖：临时脚本总是更快，所以我总是选它。代价是 8 次 exec、1 次 400、7 个 /tmp 垃圾，以及一次真实的信息差——第一版报数用的是没加拆分的口径，导致连续两轮口径沟通。
- **Repeat Risk**：高。A4（回读 5 项含分类）是**第三日复发**（9/16、9/17、9/18），A1（临时脚本 vs 默认路径）与 9/12、9/16、9/17 同族。A5（对账无判定）是新类型。A2（口径并列）是新类型但形态与「正确的规则用错方向」同族。
- **Memory Update**：① 新增守卫 P/Q/R/S/T；② 09-18 区块补全（6 条新建 + 3 条 update + 新增9/消失3/字段变化28 + 4 条矛盾记录）；③ 硬规则「执行方式」改指 audit.py，cfo_stat.py 废弃回收；④ 9/18 快照 322/265/0/57、max=361、缺口 39；⑤ cron 挂账第 29 天 + 9/17=219.6s + **首次拿到 error 实证**；⑥ remote 更正为 94yifan/maclaw-skills、push 已追平、脚本仍未入 git。
- **Principle Update**：「识别 ≠ 修复」再进一步：**加校验和加记录一样，要先算它的噪声比和它挡住的是不是下一个错误。** 表级扫描命中 90 条里 0 条需动作，等于把真异常埋掉——坏的校验会污染同一条输出里的好校验。
