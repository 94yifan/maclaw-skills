# Dreaming 复盘 2026-09-24（周四）

## 0. 今日概况（事实层）

- **今日 CFO 零业务交互**：`cfo/memory/` 无 2026-09-24 文件；`~/.openclaw/agents/cfo/sessions/` 今日唯一写入是本次 22:05 启动的 dreaming 运行（1b07f166）。无飞书收发、无礼单录入、无合同或审账动作。
- **中秋礼单表实测（9/24 22:05 audit.py，绝对路径运行）**：总记录 **328**（顶层 314 + 子 14）| 地址复核确认 **275**（顶层261+子14）| 明确否 **0** | 未处理 **53**；已邮寄/交付 **250** | 已打包 **275** | 有邮寄编号 **276**。序号 max=**368** | 缺口 **40 个**。变更对账 vs snap_2026-09-23：**新增 0 / 消失 0 / 字段变化 0**。
- **数据质量**：已确认但无收件信息（矛盾）**4 条**（5/264/300/308）；收件信息含换行 **16 条**；同电话重复 1 组（异常0/预期1）；同地址疑似重复 6 组（异常5/预期1）。全部与 9/23 相同，无变化。
- **cron 实读**（`cron_jobs` 表）：consecutive_errors=**6**，lastRunAtMs=2026-09-23 22:05:02，status=**error**，last_error=`cron: job execution timed out (last phase: model-call-started)`，durationMs=**300186**（撞 300s 上限）。三项配置零变化：payload_timeout_seconds=**300**、failure_alert=**空**、delivery_mode=none、schedule_expr=`1 22 * * *`。**挂账第 34 天。**
- **备份链路**：workspace 与 `github/main` 已对齐（ahead/behind = **0 / 0**）；`tmp/giftlist/audit.py`、`rec_api.py`、`findmany.py` 已在 git 跟踪中 → **B6②（脚本无备份出口）已闭环**；B6①（remote 名称）已由 9/18 确认为 **94yifan/maclaw-skills**。B6 三项至此全部闭合，无需再请逸凡授权。
- **触发时间漂移**：本轮 22:05 启动（schedule `1 22`，实际晚约 4 分钟），延续 9/21–9/23 的 22:04–22:05 排队窗口特征。

## A. 执行错误

**A1（P0）：cron 连败升级为 6 次，且失败签名已从偶发变为结构性。**

实读序列（`cron_jobs` + 快照时间戳，非推断）：

| 轮次 | runAt（北京） | 时长 | 结果 | 产出是否落盘 |
|---|---|---|---|---|
| 最后一次成功 | 9/19 22:15（重试轮） | 56.5s | ok | 是 |
| 9/20 22:04 | 239.9s | error（内容已落盘） | 是 |
| 9/21 22:04 / 22:24 / 22:49 | 各 300.2s | error timeout ×3 | 否（三连全丢） |
| 9/22 22:05 | 300.2s | error timeout | 否 |
| **9/23 22:05** | **300.2s** | **error timeout** | **是**（dreaming 文件 22:08、MEMORY 已更新，均早于 22:10 截断） |

关键变化：9/23 是本轮连败中**第一次落在 300s 上限但内容完整落盘**的一轮。原因是 9/23 按守卫W（落盘前置）把写动作排在了收尾模型调用之前，文件 22:08 已落，截断发生在之后的阶段。**守卫W 在真实故障场景中首次生效并被验证。** 但运行状态仍记为 error、consecutive_errors 递增 —— 说明落盘前置解决的是数据丢失，不解决运行状态与告警缺失。

**A2（P1，本轮最高价值发现）：守卫K 字段级对账存在盲区，已实测漏掉一次整类字段变化。**

9/23 的记忆与复盘记载「有邮寄编号 **251**」，本轮实读为 **276**（+25）。同时记录级与字段级对账均报「新增 0 / 消失 0 / 字段变化 0」。两者同时成立只有一个解释：**`audit.py` 的 `snap_fields()` 只落 7 个字段（名字/公司/收件信息/电话/分类/地址复核/份数备注），不含 邮寄编号 / 已打包 / 已邮寄·交付 三个进度字段。** 逸凡 9/23 夜间至 9/24 期间批量填写 25 个邮寄编号，对账完全看不见。

这是守卫K 上线（9/17）以来第一次拿到「真实发生但被漏掉」的实例，而不是推断。风险面明确：**这三个进度字段正是逸凡单方高频批量修改的对象**（邮寄编号、打包、交付三列都是她在表内逐条打勾/填写），而它们恰恰不在对账范围内。已在本轮修复（见 B1）。

**A3（P2）：已知数据质量问题零进展。** 矛盾 4 条、换行 16 条、同址异常 5 组，与 9/23 完全一致。按守卫Q（噪声比高于 20% 不做表级扫描）不逐条推，但连续多日无动作本身就是结论。

## B. 规避方案

**B1 扩 `snap_fields()`，把三个进度字段纳入快照与对账（本 session 已执行）。** 只扩快照字段不足以闭环，必须同时让 diff 输出这三个字段的变化。落地动作：`audit.py` 的 `snap_fields()` 增加 `邮寄编号` / `已打包` / `已邮寄/交付`，验证方式为 py_compile + 实跑 + 确认新快照含三字段，并确认 diff 的「表内他改」段能报出此类变化。**依据：A2 是实测漏检，不是假设。**（本轮验证输出见文末。）

**B2 cron 三项：挂账第 34 天，不再单独推送，并入 9/28（周一）全量清单。** 本 session 对 `cron update` 无权限（9/23 已第三次拿到固定原文 `Cron tool is restricted to the current cron job.`，作业存于 `~/.openclaw/state/openclaw.sqlite` 的 `cron_jobs` 表）。修复入口唯一：主 session 或人工。闭合选项：timeoutSeconds 300→600、failureAlert(after:3, mode:announce) → 回「改」即执行；schedule 频率需一句确认。

**B3 落盘前置（守卫W）继续执行，并在本 session 强化为固定顺序**：先写 dreaming 文件与 MEMORY 区块，再做代码修改与自证输出，最后回填验证结果。9/23 的实测已证明该顺序能把「撞 300s 上限」从数据丢失降级为状态错误。

**B4 备份链路 B6 三项全部闭合，从挂账清单移除。** 依据：git ahead/behind = 0/0；三个工具脚本已入跟踪；remote 名称 9/18 已确认。

## C. 书写质量

- 今日零对外回复，**无风格样本**。
- 本复盘文件自查：无装饰性引号、无破折号、无 AI 填充词；结论加粗、分层编号。A2 中引用字段名与数值均来自本轮实际读取。
- 可优化点：9/23 复盘引用的 `有邮寄编号 251` 属**未核实的沿袭值**（沿用了 9/18–9/20 同一栏位的旧数），是 A2 得以发生的书写侧原因 —— 状态数字必须当轮实读，不能跨日沿袭。

## D. 改进计划

1. **本轮**：扩 `snap_fields()` 纳入 邮寄编号 / 已打包 / 已邮寄·交付，并实跑验证（py_compile + audit 跑通 + 新快照字段确认 + diff 能报进度字段变化）。目标：明天起，逸凡批量填邮寄编号会被对账自动报出。
2. **9/28（周一）全量清单**：并入 cron 三项闭合选项 + 存量未闭环项（矛盾 4 条、115 KK、费老、麦昆、哟哟 257、352 vs 17 cherry、9/21 银行流水 7 笔 627,505.00 付款方名称错配、368 公司分类留空）。
3. **不再做**：单独重复推送 cron 三项；dreaming 内做 README 级排查（git/备份，B6 已闭合）。
4. **节奏要求**：所有跨轮引用的状态数字（邮寄编号等）当轮实读，禁止沿袭上一轮记载。

## 附：本轮验证输出（守卫N/T 自证）

- `audit.py` 实跑：`总记录 328 | 已勾 275 | 明确否 0 | 未处理 53`；`批量进度: 地址复核确认 275(顶层261+子14) | 已邮寄/交付 250(顶层236+子14) | 已打包 275(顶层261+子14) | 有邮寄编号 276(顶层262+子14)`；`序号 max=368 | 缺口 40`；`变更对账 vs snap_2026-09-23.json: 新增 0 / 消失 0 / 字段变化 0`。
- `cron_jobs` 实读：consecutive_errors=6，last_run_at_ms=1790172302222（=2026-09-23 22:05:02 CST），status=error，duration=300186ms，error=timeout(model-call-started)；payload_timeout_seconds=300、failure_alert_after=空、delivery_mode=none、schedule_expr=`1 22 * * *`。
- git 实读：`git rev-list --left-right --count github/main...HEAD` = `0	0`；`git ls-files tmp/giftlist/` 含 audit.py / rec_api.py / findmany.py。
- 快照字段实读（`snap_2026-09-24.json`）：字段集 = 份数备注 / 公司 / 分类 / 名字 / 地址复核 / 收件信息 / 电话 —— **确认不含 邮寄编号 / 已打包 / 已邮寄·交付**，A2 根因成立。

## 知识缝合 [2026-09-24]

- 守卫K（字段级对账） → 撞上 A2 = **对账范围必须覆盖「谁在改什么」而不仅是「改了哪条」**：逸凡改的是进度字段，我的快照存的是身份字段，集合不交则对账恒为「无变化」。设计对账时先问「第二个写者最常动哪几列」，把那几列优先纳入，而不是把自己关心的字段纳入。
- 守卫W（落盘前置） → 9/23 实测 = **从「该做」升级为「已验证」**：同一故障条件（撞 300s 上限）下，落盘顺序决定结果是「状态 error 但数据在」还是「状态 error 且数据无」。守卫的价值可被单次对照实验证实，不必等到多次样本。
- 守卫N/O（已编译须自证 / 修不留到明天） → 撞上 B6 = **挂账项要区分「需授权」与「我自己能做」**：B6 三项里两项（脚本入跟踪、remote 确认）本就无需求人，挂了 6 天是因为被和「需授权」的第三项打包在一起推送。归类错误会制造伪阻塞。
- 守卫V（可达性须带坐标） → cron 修复 = **「无权限」结论也要给出替代入口坐标**：9/23 已固定「作业在 sqlite 的 cron_jobs 表 + 唯一入口是主 session」，使本轮无需重复搜寻即可确认，坐标化使重复成本归零。

## Reflection

- **Goal**：复盘本身达成；今日零业务，无用户问题待解决。真正未达成的是 dreaming 的运行健康（连败 6 次），但本轮的增量在于把守卫W 从假设变成实证、把守卫K 的盲区从推断变成实测。
- **Key Decision**：以「9/23 记载 251 vs 本轮实读 276 且对账报无变化」为切入点，反推出快照字段集缺三个进度字段，而不是把差异当成笔误抹掉。判断依据是可复现的两条读数与快照文件的实际字段集。
- **Main Mistake**：9/23 沿用了上一轮的 邮寄编号 251（未当轮实读），直接掩盖了一次真实的表内变化；同类沿袭在 9/18–9/20 也存在，属系统性书写习惯而非一次笔误。
- **Repeat Risk**：是。跨轮沿袭状态数字是高频问题（9/17 B3、9/19 守卫U、9/23 沿袭值，同一族）；cron 连败属外部约束（无权限）但损害持续累积，只能靠 9/28 全量推送收口。
- **Memory Update**：见 MEMORY.md 新增 2026-09-24 区块（表结构实测、A2 根因与修复、B6 闭合、挂账第 34 天）。
- **Principle Update**：新增**守卫X 对账范围按第二写者定标** —— 变更对账的字段集不由「我关心的字段」决定，而由「另一个写者最常修改的字段」决定；上线对账时先列第二写者的高频写入列，逐一纳入快照，未纳入即为已知盲区并须显式记录。依据 2026-09-24 A2 实测漏检。
