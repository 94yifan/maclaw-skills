# Dreaming 复盘 2026-09-23（周三）

## 0. 今日概况（事实层）

- **今日 CFO 零业务交互**：workspace 无当日 memory 文件；`~/.openclaw/agents/cfo/sessions/` 今日唯一写入是本次 22:05:02 启动的 dreaming 运行（0d345ff5）。无飞书收发、无礼单录入、无合同或审账动作。今日新增文件只有本轮 audit 生成的两份快照。
- **中秋礼单表实测（9/23 audit.py，绝对路径）**：总记录 **328**（顶层 314 + 子 14）| 地址复核确认 **275**（顶层261+子14）| 明确否 **0** | 未处理 **53**；已邮寄/交付 **250** | 已打包 **275** | 有邮寄编号 **251**。序号 max=**368** | 缺口 **40 个**。变更对账 vs snap_2026-09-22：**新增 0 / 消失 0 / 字段变化 0**。
- **数据质量**：已确认但无收件信息（矛盾）**4 条**（5/264/300/308）；收件信息含换行 **16 条**。同电话重复 1 组（异常0/预期1）；同地址疑似重复 6 组（异常5/预期1）。
- **cron 实读**：consecutive_errors=**5**，lastRunAtMs=2026-09-22 22:05:02，status=error，errorReason=timeout，durationMs=**300164**（撞 300s 上限）。三项配置零变化：payload_timeout_seconds=**300**、failure_alert=**空**、delivery_mode=none、schedule_expr=`1 22 * * *`。**挂账第 33 天。**
- **触发时间漂移**：9/21 实际 22:04:19、9/22 22:05:02、本轮 22:05:02，均比 `1 22` 晚约 4 分钟（22:00–22:06 是多分身 dreaming 排队窗口）。

## A. 执行错误

**A1（P0，本轮最高优先级）：Dreaming cron 连续 5 次失败，9/21 与 9/22 两天零产出，记忆链路断两天。**

实读运行序列（`cron runs` + `cron_jobs` 表，非推断）：

| 轮次 | runAt（北京） | 时长 | 结果 |
|---|---|---|---|
| 最后一次成功 | 9/19 22:15（重试轮） | 56.5s | ok |
| 9/20 | 9/20 22:04 | 239.9s | error（Process kind-ember failed，**内容已落盘**） |
| 9/21 首轮 | 9/21 22:04 | 300.2s | error timeout |
| 9/21 重试1 | 9/21 22:24 | 300.2s | error timeout |
| 9/21 重试2 | 9/21 22:49 | 300.2s | error timeout |
| 9/22 | 9/22 22:05 | 300.2s | error timeout |

last phase 全部为 `model-call-started`。**后果（已发生的实际损害，不是趋势推断）：**

1. **9/21、9/22 无 dreaming 文件、无 MEMORY 更新** —— 复盘链路整两天为零。
2. **9/21 一晚三轮全量重跑**（22:04 / 22:24 / 22:49）。超时后调度器的重试不是续跑，是**从零重读** SOUL + MEMORY + 业务会话再跑一遍：三个独立 session（46bbfa65 / 06d2d963 / 08630279），约 15 分钟纯重复 token，全部产出为零。
3. **9/22 的业务动作未进长期记忆**：164 改名 Doris + 叶姐收件信息、203 洪老师复核打勾、368 飞虎（名字列改虎哥）。这三笔只存在于会话日志里，MEMORY.md 里查不到。本轮已在 MEMORY 补写。

三连超时期间三个 session 的最后动作分别是：查 `cfo/tmp` 的 git 跟踪状态、跑回复风格违规检查、翻 toolResult 原始结构 —— 都卡在 model-call-started 被杀，属上下文过重 + 300s 上限的直接结果。

**A2（P1）：9/22 新增 368 的回执漏报第二个读数。**

9/18 逸凡定下的硬规则：任何 create/update 回执必须附「现在确认了多少」+「邮寄了多少」两个实时读数。9/22 12:45 新增 368 的回执只写了「全表 328 条（顶层314+子14），地址复核确认 275」，**缺 已邮寄/交付**。`rec_api.py after_write()` 已自动打印两个数，是起草回执时删掉了一个 —— 规则在默认路径里生效、在我这最后一跳失效。

**A3（P1，跨日）：写入顺序错误是这两天全丢的直接原因。**

9/19 那轮侥幸保住，是因为落盘（dreaming 文件 22:06:25、MEMORY 22:06:43）排在收尾模型调用之前；9/21 三连超时的三轮全部把编辑动作排在深度分析之后，于是全丢。**根因不是运气，是排序。** 不可重建的写动作没有优先级。

**A4（P2）已知但未闭环的数据质量问题无进展**：矛盾 4 条、换行 16 条、同址异常 5 组，连续多日不动（噪声比高，按守卫Q 不逐条推）。

## B. 规避方案

**B1 落盘动作前置（本 session 已执行）。** 本轮改顺序：先写 dreaming 文件与 MEMORY 区块，再做取证补全与自证输出。并把这条写成 dreaming 的固定执行顺序（见 MEMORY 守卫W），不只写在复盘里。

**B2 cron 三项修复：本 session 第三次被拒，已固定原文证据，不再第四次推送。**

9/23 本轮实跑 `cron update`（jobId `07cd1aef-08d4-4109-a178-a20c8adc8209`，patch：timeoutSeconds 300→600 + failureAlert after:3 announce），返回原文：

```
Cron tool is restricted to the current cron job.
```

结论固定：isolated cron session 只能 `remove` 自己，改不了自己的配置；作业存于 `~/.openclaw/state/openclaw.sqlite` 的 `cron_jobs` 表，外部写有网关一致性风险且未获授权。**唯一入口是主 session 或人工。** 处置：并入下一周一（9/28）全量清单的闭合选项，附本轮实测拒绝原文；在此之前不再单独重复推送。

**B3 降低单轮取证工作量（本 session 可做）。** 9/21–9/22 三连超时的 session 都在做重活。修正：业务会话取证只抽 user/assistant 文本（不翻 toolResult 结构、不做结构探查）；礼单读数固定用 `audit.py` 一行输出，不手搓脚本；README 级排查（git 跟踪、备份链路）不放进 dreaming 主流程。

**B4 记忆缺口回填已完成。** 9/21 的返点工作已在业务会话内落地 MEMORY（18:28 那次编辑）；9/21–9/22 的复盘缺口与 9/22 三条表内动作由本轮补写。

## C. 书写质量

- **9/21 业务 5 条回复：风格违规 0**（当轮自检输出 `回复1–5 违规:无`）。
- **9/22 业务 6 条回复：违规 0**（本轮实跑扫描，无装饰引号、无破折号、无 AI 填充词）。
- 连续两日零风格违规。唯一质量缺口是 A2 的读数遗漏，属内容完整性而非风格。
- 本复盘文件自查：无装饰性引号、无破折号、无 AI 填充词；结论加粗、结构化分层。

## D. 改进计划

1. **本轮**：落盘前置已执行（顺序：MEMORY 与 dreaming 文件先写，分析后补）。已把该顺序写成守卫W。
2. **MEMORY.md 补写**：9/21（返点对账 + 银行流水）、9/22（礼单三条动作 + create 读数遗漏）、9/23（连败实读 + 守卫W）三个区块。
3. **下次全量清单（9/28 周一）**：并入 cron 修复闭合选项（回改即由主 session 执行）+ 9/22 遗留检查项。
4. **不再做**：第 4 次单独推送 cron 三项；dreaming 内做备份链路/结构排查。

## 附：本轮验证输出（守卫N/T 自证）

- `audit.py` 实跑（完整输出已落快照 `snap_2026-09-23.json`）：`总记录 328 | 已勾 275 | 明确否 0 | 未处理 53`；`批量进度: 地址复核确认 275(顶层261+子14) | 已邮寄/交付 250 | 已打包 275 | 有邮寄编号 251`；`序号 max=368 | 缺口 40`；`变更对账 vs snap_2026-09-22.json: 新增 0 / 消失 0 / 字段变化 0`。
- `cron runs` 实读：连续 5 条 error，时长 239953 / 300168 / 300182 / 300186 / 300164 ms。
- `cron update` 实测返回原文：`Cron tool is restricted to the current cron job.`
- 风格扫描脚本实跑（9/22 会话 90299c82）：6 条回复全部 `违规=无`。

## 知识缝合 [2026-09-23]

- 守卫O「修代码不留到明天」 → 撞上 9/21–9/22 三连超时 = **修复窗口的成本随时间指数上升**：发现的当轮修，成本一次命令；隔两天修，要先重建两天的上下文。
- 守卫S「两次读数间表变了要报增量」 → 跨轮版本 = **同日重试是高成本的伪增量**：重试轮读到的数据没变，但消耗了整轮预算。分母修正：9 月 dreaming 24 跑 15 error（62.5%），有效产出率比表面成功率更低。
- 守卫U「回执读数来自写之后」 → 9/22 首次违反 = 规则编译进脚本只保证「数值可得」，**不保证「被采用」**。最后一跳（起草回执）仍是人（我）的判断点，需要把两个读数直接嵌进模板句而非留在工具输出里。

## Reflection

- **Goal**：复盘本身达成；今日零业务，无用户问题待解决。真正的目标（让 dreaming 不再丢）连续两天未达成，且是这两天的最大损失来源。
- **Key Decision**：把不可重建的落盘动作提前到深度分析之前。依据是 9/19 侥幸保住 vs 9/21 三轮全丢的对照。
- **Main Mistake**：不是今天的失误，是 9/21–9/22 的连续全丢（根因：排序错误 + 300s 上限 + 重试是重跑），以及 9/22 create 漏报读数。
- **Repeat Risk**：是，且已升级。cron 连败从「偶发」变为「连续 5 次」；A2 是 9/18 规则落地 4 天后的首次违反 —— 两者都是系统性缺口，不是一次性失误。
- **Memory Update**：见 MEMORY.md 新增 9/21、9/22、9/23 三区块（返点对账实据、礼单三条表内动作、连败实读与拒绝原文、守卫W）。
- **Principle Update**：新增**守卫W 落盘前置** —— 凡可能因超时被中断的长任务，把不可重建的写动作（长期记忆、复盘文件）排在深度分析与取证之前。依据 9/21 三连超时零产出 vs 9/19 先落盘侥幸保住。
