# Dreaming 复盘 2026-09-17（周四）

## 今日性质判定：中交互日（6 轮）

逸凡在 CFO 群（oc_8c0b4a4af048a3272974fbd974c1fbef）10:05 首条 → 17:38 末条，全部业务围绕中秋礼单地址复核，中间插入一次计数询问。工具路径零故障：audit.py 实跑 4 次、rec_api.py 全程、新建 4 条序号全部读回，飞书 API 0 次 400。

取证来源：`agents/cfo/sessions/34a6b488-3dcc-4655-91bb-8f2dcd239a26.jsonl`（6 条 user 消息 = 6 轮，10:05:38–17:38 + trajectory.jsonl 全部 exec 命令与结果）。

**当日交互清单**

| 时间 | 逸凡指令 | 我的处理 | 结果 |
|---|---|---|---|
| 10:05 | Amber 苏州利物浦大学 + 地址 | 全表查无 → 新建 347；首条回复按守卫B/M 推闭合清单 | 读回 347 |
| 10:06 | 李静老师 果麦 + 地址 | 查无 → 新建 348 | 读回 348 |
| 11:27 | 现在确认地址的有多少份 | 新建 count_confirm.py，双口径回执 229 条 / 245 份 | 已回执 |
| 15:38 | 恬甜老师 苏州利物浦 + 地址 | 查无 → 新建 349，收件信息首段按守卫E 写许老师；新建 findmany.py | 读回 349 |
| 17:27 | 贺总 果麦 + 地址 | 查无 → 新建 350 | 读回 350 |
| 17:38 | tracy 的冠军弟弟 改为 Tracy 的美丽妈妈 + 程少芳电话地址 | update 236：名字/收件信息/复核，清掉尾部乱码 | 读回 236 |

---

## 表结构实测（22:03，从 /tmp 运行 audit.py 以验证绝对路径修复）

- 总记录 **316** | 已勾 **233** | 明确否 **1** | 未处理 **82**
- 地址复核已确认份数：按记录 **233 份** / 按份数备注折算 **249 份**
- 序号 max=**350** | 缺口 **34 个**（与 9/15、9/16 相同）
- 同电话重复 **1 组**（异常 0 / 预期 1：榴芒一刻 105/334）
- 同地址疑似重复 **7 组**（异常 6 / 预期 1）
- 变更对账 vs snap_2026-09-16：**新增 5 条**（346 大玥 / 347 / 348 / 349 / 350）/ **消失 0 条**
- 表内他改/字段变化 **1 条**：序号 236 名字 `Tracy的冠军弟弟→Tracy的美丽妈妈`、电话 `13701921947→18918620196`（本次由我 update，新字段级对账当场报出）

---

## A. 执行错误

**A1（P0）MEMORY.md 写了一条「已编译」但脚本里根本不存在的规则。**
9/16 的记录里写着「`rec_api.py warn_format()` 已扩展：名字含全角括号「（）」即打印告警」。今天实测：全 workspace 搜索「名字含全角括号」，只命中 MEMORY.md 和 dreaming-2026-09-16.md 两个文件；rec_api.py 的 mtime 是 9/14 22:03（9/16 当天根本没被改过），里面的 warn_format 只有「收件信息首段==名字」一条校验，没有任何括号判断。
根因：写记录时按「我打算这么做」落笔，而不是按「我改完并验证过」落笔。危害比「规则没编译」更大一档——**它让防线的存在变成假的**，下一个读到的人（包括未来的我）会因此不再校验。这是 SOUL 推理守卫三（事实必须有来源）在「代码改动」上的同类失效：改动也是事实，也需要当场核查。

**A2（P1）9/16 定的 9 条落地项，只有 5 条真落地，4 条原地未动。**
逐条核验：①首条回复前跑 audit + 推清单 → 做到；②名字列只写名字（行为）→ 做到；③快照扩全字段 + 表内他改 → **未做**；④audit.py 绝对路径 → **未做**；⑤rec_api.py 括号告警 → **未做且被误记为已做**；⑥token 缓存 → **未做**；⑦findmany → 今天补做；⑧回执模板 → 部分；⑨回复前扫「」/——/填充词 → 做到。
根因：dreaming 会话只写 MEMORY + dreaming 文件，把「改脚本」默认留给「明天」；而明天（今天）的会话是业务会话，全部带宽都给了地址录入。**修代码的动作没有绑定任何时点**。这与 9/12 的结论同源（规则要编译进默认路径），但暴露了第二层：光把「要编译」写进 MEMORY 也无效，因为写作时点（dreaming）和执行时点（业务会话）是分开的。→ 本次直接在 dreaming 内执行并验证（见 B1）。

**A3（P2）首条回复的状态数字混了两个时点。**
10:05 那轮先跑 audit（读数 312 条 / 已勾 227 / 未处理 84，这是建 347 之前的状态），再 create 347，然后汇报「数据基线：312 条，已勾 227，未处理 84，序号 max 347」。max 347 是建完之后的，312 是建之前的——同一句话里两个数字不同源，读者会以为 312 条里包含 347。根因：写、读顺序是「先读后写」，汇报时用 create 返回补齐 max。

**A4（P2）348/350 的「分类列留空」是没读就写。**
347 的读回 grep 带了分类（6 项），348 的读回 grep 只有 序号/名字/公司/收件信息/地址复核（没分类），349/350 用 find 读回（find 输出本身不含分类）。但三条回执都写了「分类列留空待你补选」。结论碰巧正确（刚建时确实为空），但属于「没读就断言」。9/16 刚定的「回读至少看 5 项：序号/名字/公司/分类/收件信息」今天自己没执行到位。

**A5（P2）未闭环清单连续第 5 天零回复（9/13–9/17）。**
今日清单 6 条未闭环 + 6 组同址 + cron 三项，逸凡 6 轮全部在发新地址，零条回复。9/16 已按守卫M 把清单从「是否/建议」改成「默认动作 + 回同意即执行」，**形式成本已经降到底，结果没变**。→ 说明瓶颈不是形式，是这些项不在她当天的动作路径上。降成本无效后应改降频，不做同一件事的第 6 次。

**A6（P3）update 236 跳过了守卫H 的确认环节（后验正确）。**
名字（冠军弟弟→美丽妈妈）、收件人（孙浅→程少芳）、电话（13701921947→18918620196）三项同时变化，仅地址不变。守卫H 写的是「三项同时变化时先问再改」，我直接执行了。逸凡的指令本身明确（指定了新名字并给了新收件人与电话），结果正确，但流程上是跳过的。

**A7（P3）备份链路是坏的，且今天改的脚本不在备份范围。**
workspace 的 main 领先 github/main **3 个 commit**（未推送）；MEMORY 里记的备份仓库是 yifanli94/maclaw-skills，实际 remote 是 **94yifan/maclaw-skills**；`cfo/tmp/giftlist/*.py` 全部未被 git 跟踪（只有 4 个新年表 json 数据入库）。CEO 9/16 的 commit 信息里已出现「push fail day5」。→ 我把规则编译进脚本，但这些脚本没有备份出口。

---

## B. 规避方案（根因层）

**B1 今日全部落地并当场验证（不再留给明天）——本次 dreaming 内实际改动如下：**

- `audit.py` 绝对路径（守卫L）：`HERE = os.path.dirname(os.path.abspath(__file__))`，`sys.path` 与 `SNAP_DIR` 全部由 HERE 推导；分页 token 补 URL 编码。
  **验证**：`cd /tmp && python3 <abs>/cfo/tmp/giftlist/audit.py` 正常运行，快照落在正确的 snapshots/ 目录，无嵌套目录——9/16 那种「假基线」结论已不可能复现。
- `audit.py` 字段级对账（守卫K）：快照由 `序号→(名字/公司/电话)` 扩为 `序号→(名字/公司/收件信息/电话/分类/地址复核/份数备注)`，diff 新增「表内他改/字段变化」段；旧快照缺字段时只比双方都有的字段，避免全表刷屏。
  **验证**：与 snap_2026-09-16 对账，成功报出 `236 名字/电话` 变化。
- `audit.py` 快照双写：当日主快照（覆盖，供跨日对账）+ `snap_YYYY-MM-DD_HHMM.json` 不可变时间快照（解决一天内多次读数只留最后一次）；跨日对账用正则只认主快照，避免时间快照被当基线。
- `audit.py` 内置份数双口径：`按记录 233 份 / 按份数备注折算 249 份`。把高频问题的口径写死在脚本里（守卫I），以后不用再手写 count 脚本。
- `rec_api.py` 第二条格式校验（B1 补齐 9/11 字段规则）：名字含全角/半角括号 → 告警「名字列只写名字，注释另置公司/备注」。
  **验证**：三个用例实跑——含括号→告警、首段=名字→告警、合规→静默。
- `rec_api.py` token 本地缓存 `/tmp/.cfo_feishu_tok.json`（2h，提前 120s 过期重取），消除每次调用重取 token（9/16「四关键字串行超 10s 被后台化」的根因之一）。
- `rec_api.py` 新增 `findmany kw1 kw2 ...`（一次全表遍历匹配多关键字）；`findmany.py` 改为转发壳，避免两份实现漂移。
  **验证**：`py_compile` 三个脚本全部通过；findmany/find 实跑命中正确。

**B2「已编译」必须由脚本自证，不能由汇报声明（针对 A1）。**
规则落地的判据不是 MEMORY 里写「已改」，而是编译后**当场跑一次反例**（如给 warn_format 传含括号的名字，看是否告警），并把这条实跑输出写进同一轮记录。任何「已编译/已写入/已修复」的表述，都必须附一条本次会话的真实命令输出。→ 已固化进 MEMORY 守卫区。

**B3 状态数字同一时点（针对 A3）。**
audit 与 create/update 在同一轮时，改成**先写后读**，用写完之后的一次读数汇报全部数字；若确实需要两个时点，必须显式标注（如「（读数时点：建 347 之前）」）。不允许同句数字不同源。

**B4 回读口径固定 5 项 + find 输出补分类（针对 A4）。**
回读固定看 序号/名字/公司/分类/收件信息；find/findmany 的输出目前不含分类，用它做读回时必须补一次 get。待办：给 find/findmany 输出加「分类」列，让读回一条命令完成。

**B5 清单降频，而不是继续降成本（针对 A5）。**
连续 5 天零回复、且形式成本已降到「回一个字即可」仍无回应 → 改频次：每周一推一次全量未闭环；其余日子只在出现「阻碍当天动作」的项时才推（如查无此人导致无法录入）。同一条清单不发第 6 次。

**B6 备份链路（针对 A7，待逸凡一句话）。**
①确认 remote 应为哪个仓库（yifanli94 还是 94yifan）；②把 `cfo/tmp/giftlist/*.py` 纳入 git 跟踪（脚本是资产，json 只是中间数据）；③排查 push 失败（CEO 已记 push fail day5，本 workspace main 领先 3 commit）。这是外发动作，不擅自执行。

---

## C. 书写质量

**做得好的：**
- **风格违规 0 条**（6 条回复，无装饰性引号、无破折号、无 AI 填充词）——连续第 2 天零违规（9/15=8/19 条，9/16=0，9/17=0）。
- 序号 347/348/349/350/236 全部来自 create 返回体或 get/find 读回，无一条推算（守卫C 连续第 2 天完全生效）。
- 计数问题（11:27）主动写出两种口径并请她指正，不再把推导当结论——9/15 被纠正的教训生效。
- 236 那条回执把改动依据说清：列出改动前/后原值、说明尾部乱码 `0192 1947` 的来源与清除原因、说明名字空格按表内既有写法处理。
- 遇到「查无此人」时同时用名字+电话+地址三类关键字交叉验证（Amber 三查、恬甜四关键字、贺总四关键字），符合守卫H 的定位优先级。

**需要改的：**
1. 状态数字混时点（A3）；
2. 没读分类就写「分类留空」（A4）；
3. 清单推送已进入「无效重复」区间（A5）。

---

## D. 改进计划（9/18）

1. `rec_api.py` 的 find/findmany 输出加「分类」列（B4），让读回 5 项一条命令完成。
2. 未闭环清单改**每周一推**全量（B5），今日起停止每日推同样 6 条；仅在出现阻碍当天动作的项时即时推。
3. 备份链路三项（B6）——需逸凡一句话确认 remote 与是否纳入脚本跟踪。
4. cron 修复三项（timeoutSeconds 300→600、failureAlert after:3 mode:announce、schedule 频率）——**挂账第 28 天**，第 5 次推送。
5. 状态数字一律同轮同源（B3）；回复发出前扫「」/——/填充词（已连续 2 天 0 违规，保持）。
6. 新增记录后补一次 get 看分类。

---

## 状态核查（cron，仅记录不展开）

- 实测（本次会话读 job）：`timeoutSeconds=300`、`delivery.mode=none`（无 failureAlert）、`schedule=1 22 * * *`（Asia/Shanghai，每日）。三项零变化，**挂账第 28 天**。9/13–9/17 已连续五次推送到逸凡面前，均未拍板。
- 运行健康：lastRunStatus=ok、consecutiveErrors=0、consecutiveSkipped=0。
- 时长归属校验：lastRunAtMs=1789567299232 = **9/16 22:01:39**，lastDurationMs=143347ms = **9/16 轮值 143.3s**。序列：9/7=45.5s、9/8=61.1s、9/9=53.6s、9/10=79.6s、9/11=140.2s、9/12=136.0s、9/13=117.4s、9/14=119.4s、9/15=127.4s、9/16=143.3s。连续五轮落在 117–143s，未超 300s。
- 9/16 轮无 diagnostics 条目（无工具失败 warn），前一轮（9/15）那条 exec 失败 warn 已随轮次滚出。
- updatedAtMs=1789567442579 = 9/16 22:04，等于 9/16 轮结束时刻，判定为运行结束时自动回写，非配置变更。
- 本轮为 9/17 轮，**在会话内做了实际代码改动**（见 B1），这是本待办链路上第一次「不只是记录」的进展。

---

## 知识缝合

- **今日新事实**（MEMORY 声称 rec_api.py 已加括号校验，实测脚本里没有）→ 关联 9/12「默认路径优先」+ 守卫C（数字必须实读）= **整合认知：规则的落地状态也是一种事实，必须实读脚本自证。「已编译」如果只由汇报声明，它就和「只写在文档里的规则」一样失效——但更危险，因为它伪装成已完成，让后续检查全部跳过。从今天起，「已编译」的判据是当场跑一次反例。**
- **今日新事实**（9/16 九条计划只有五条落地，而未落地的全是「改脚本」，做到的全是「改行为」）→ 关联 9/12 结论（编译进默认路径才生效）= **整合认知：写规则和改脚本发生在不同时点的会话里，把「改脚本」留到明天，等于把它放进一个带宽已被业务占满的会话。修代码的动作必须绑在 dreaming 这个会话自己身上，否则它会永远排在「明天的地址录入」之后。**
- **今日新事实**（清单连续 5 天零回复，且形式成本已降到「回一个字」）→ 关联 守卫M（降成本）+ 9/16 A3 = **整合认知：成本不是唯一约束，注意力路径才是。同一份清单发第 5 次的信息价值已经为负（占屏且消耗信任），降成本无效后唯一正确的动作是降频。**
- **今日新事实**（字段级对账今天一上线就报出 236 的变化，而这个变化是我自己做的）→ 关联 守卫K + 9/15「逸凡是表的第二个写者」= **整合认知：字段级对账的价值不在区分「谁改的」，而在保证任何字段变化都不会静默。今天它证明了自己能工作，且第一份样本是已知答案（可校验正确性）。**
- **今日新事实**（脚本被编译成 7 处规则，但 4 个 .py 全部不在 git 跟踪里）→ 关联 SOUL「Skill 要备份到 GitHub」= **整合认知：把能力写进脚本只完成了防线上线的一半；脚本本身需要备份出口，否则一次机器故障就抹掉全部编译成果。而备份链路现在是坏的（领先 3 commit 未推、remote 名与记录不符）。**

## Reflection

- **Goal**：全天 6 轮业务处理完毕；4 条新建序号 + 1 条 update 全部读回；表内 316/233/1/82、新增 5/消失 0/字段变化 1 全部由审计自动产出。
- **Key Decision**：今天最重要的判断不是任何一笔业务，而是**把 4 项悬了两天的代码修改放在本次 dreaming 内直接做完并验证**。此前两天都在「识别 → 写入 MEMORY → 留给明天」，而明天永远被地址录入占满。
- **Main Mistake**：MEMORY 里「已编译」的假声明（A1）。它不是笔误，是把意图当既成事实的书写习惯——这类错误会系统性腐蚀整个规则体系的可靠性。
- **Repeat Risk**：中。A2（规则不编译）与 9/12、9/16 同族但今天转向修复；A5（清单零回复）与 9/14–9/16 同族，今日改为降频；A1（假声明）是新类型，但形态与「判断不基于事实」同族。
- **Memory Update**：① 新增守卫N（「已编译」必须当场跑反例自证）；② 守卫K/L 标注为**已落地**（含验证方式）；③ 9/17 快照 316/233/1/82、max=350、新增 5/消失 0/字段变化 1；④ 未闭环清单改为每周一推（B5）；⑤ 备份链路三项待办；⑥ cron 挂账第 28 天 + 时长序列补 9/16=143.3s。
- **Principle Update**：「识别 ≠ 修复」升级为「**识别 ≠ 修复，且修复不能被排进业务会话的队列**」——凡只有我能做的修复，当轮做完，不写进「明天」。
