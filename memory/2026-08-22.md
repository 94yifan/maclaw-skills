# 2026-08-22

## 飞书消息格式规则变更（重大）

逸凡指出我发的消息完全看不到结构（编号、板块、加粗标题都没了）。

**根因**：飞书 streaming 此前被关（channels.feishu.streaming=false），消息走普通文本，markdown 不渲染；而 SOUL 里基于纯文本前提写的"不用星号、不用清单体、描述性段落"规则被执行过头，导致输出变成纯文字堆砌。

**处理**：
1. 开启 channels.feishu.streaming=true（直接改 ~/.openclaw/openclaw.json，config.patch 对 channels 路径受保护；改前已备份 openclaw.json.bak.streaming），热重载生效，逸凡确认能看到加粗和编号。
2. 更新主 SOUL.md 三处：推理守卫四语言层/符号层、Skill 汇报格式、协作说人话零AI味——强调用真加粗（卡片渲染），允许编号列表、板块分区、加粗标题；保留禁止引号概念强调和 AI 填充词。
3. 更新 MEMORY.md 逸凡汇报风格第 4 条：描述性段落改为结构化表达。
4. 同步 7 个 subagent workspace 的 SOUL.md：brain-mining、ceo、cfo、strategic-planner、supermind（统一版本）、social-crawler、lanshi（各自变体）。

**逸凡的沟通偏好（重要，写死）**：重要内容必须结构化——编号、板块分区、核心标题加粗。这是她明确要求的，不是可选项。

## Codex Harness 讨论

逸凡发了新智元文章（OpenAI 开源 Codex Harness，Apache-2.0），问能不能拿来用/增强我们自己的 harness。

**查证结论**：
- openai/codex 仓库真实（2025-04-13 创建，Rust+Python，Apache-2.0 已核实）
- OpenClaw 已内置 codex harness 作为可选 runtime（plugins.entries.codex + openai/gpt-* 模型 + OAuth 订阅/API key），不需要移植代码
- 职责划分：Codex 管原生模型循环/工具延续/native compaction（上下文压缩更强，ARC-AGI-3 数据），OpenClaw 管飞书、session、动态工具、审批、媒体、记忆体系（MEMORY.md 投影给 Codex turn）
- 硬约束：绑定 OpenAI 模型，有成本；DeepSeek 默认铁律不受影响（可只给特定任务配 openai 模型）
- 待逸凡决策：是否试点（需要 ChatGPT/Codex 订阅或 OpenAI API key）
